%%  Ex1.b

% O algoritmo de Euler diz-nos que para calcular o valor seguinte temos 
% de utilizar o valor anterior + a expressão*passo(intervalor de tempo)
% 
% Não se pode aplicar diretamente este algoritmo uma vez que este algoritmo 
% só é valido para equações diferenciais de 1ª ordem, deste modo como a 
% equação obtida em a) é de segunda ordem não se pode aplicar a mesma.